export enum COLUMN {
  DATE = 4,
  STATUS = 7,
  BRAND = 8,
  AUTH_AMOUNT = 14,
  SETT_AMOUNT = 15,
  SURCHARGE = 16,
  DEPARTMENT = 17,
  CONTROL = 18,
}

export enum BRAND {
  AMEX = 'AMEX',
  VISA = 'VISA',
}

export enum STATE {
  APPROVED = 'APPROVAL',
  SETTLED = 'SETTLED',
}

export enum PREFIX {
  AMEX = 'AX',
  DEFAULT = 'CC',
}

export enum SUFFIX {
  APPROVED = 'AP',
  DEFAULT = '1',
}
